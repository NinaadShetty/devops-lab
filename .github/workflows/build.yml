name: Build Pipeline

on:
  push:
    branches:
      - 'IN-1-Integrate-NPM-Project-with-DevOps-Integration'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Display Build Message
        run: echo "GitHub Actions Build Successful"
